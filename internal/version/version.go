package version

var (
	// GitVersion is the version metadata of the commit at which this binary was built.
	GitVersion = "unknown"

	// GitCommit is the full commit SHA at which this binary was built.
	GitCommit = "unknown"
)
